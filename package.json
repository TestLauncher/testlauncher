{
  "name": "@testlauncher/public-examples",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test && python3 -m unittest discover -s examples/bugagent/api-quickstart -p 'test_*.py'",
    "check": "node scripts/check-examples.mjs"
  }
}
